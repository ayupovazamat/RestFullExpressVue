export enum ObjectTypeEnum {
  sale = "продажа",
  rent = "аренда"
}

export enum ObjectGarageType {
  garage = "гараж",
  "parking place" = "машиноместо",
  "box" = "бокс"
}

export enum ObjectInitAreaEnum {
  m = "m2",
  s = "сот."
}

/*export function ObjectGarageType(type) {
  if (type == "гараж" || type == "garage" || type == 1) return 1;
  if (type == "машиноместо" || type == "parking place" || type == 2) return 2;
  if (type == "бокс" || type == "box" || type == 3) return 3;

}*/

/*«гараж»/«garage»
  «машиноместо»/«parking place»
  «бокс»/«box»*/